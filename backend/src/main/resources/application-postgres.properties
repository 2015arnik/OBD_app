spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost:5432/obd}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${POSTGRES_USER:obd}
spring.datasource.password=${POSTGRES_PASSWORD:obd}

spring.h2.console.enabled=false
